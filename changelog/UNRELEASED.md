# Unreleased

## Planned

- native XLSX import with record preview and column mapping;
- live data-bound tables and charts;
- batch export for generated pages, records and campaign variants;
- persistent governance log for export overrides and approvals;
- drag-to-position comment pins, rectangular regions and notifications;
- keyboard/touch page ordering and persistent thumbnail cache;
- integrated asset picker for semantic image and logo fields;
- one-click document-wide brand migration and logo replacement;
- Playwright editor journeys and screenshot visual regression;
- export fidelity and golden-file suite;
- live-linked campaign variants;
- command palette and searchable shortcut reference;
- plugin/extension SDK and public integration contracts.

Do not list video, audio, animated timeline or 3D work here: those areas are outside product scope.
